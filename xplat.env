# xplat Tool Versions
#
# Single source of truth for all tool versions managed by xplat.
# Used by: Taskfiles, CI workflows, local development
#
# Format: KEY=VALUE (no spaces around =)
# Usage in Taskfile: {{.TASK_VERSION}} after loading via dotenv
# Usage in GitHub Actions: source xplat.env && echo $TASK_VERSION

# Task (go-task) - https://taskfile.dev
TASK_VERSION=3.45.5

# Go - https://go.dev (matches go.mod requirement)
GO_VERSION=1.25

# Go strict mode - if true, toolchain:golang:check:deps fails on version mismatch
# Set to true for stricter CI enforcement
GO_VERSION_STRICT=false

# xplat - cross-platform CLI (built from this repo)
XPLAT_VERSION=v0.2.0

# Bun - https://bun.sh
BUN_VERSION=1.3

# Binary tools (use xplat binary:install)
ANALYTICS_VERSION=v0.1.0
SITECHECK_VERSION=v0.1.0
GENLOGO_VERSION=v0.1.0
TRANSLATE_VERSION=v0.2.0
MAILERLITE_VERSION=v0.1.0

# Development tools (built from this repo, wrapping upstream libraries)
PROCESS_COMPOSE_VERSION=v0.1.0
TASK_UI_VERSION=v0.1.0

# External tools (downloaded from their GitHub releases or npm)
GH_VERSION=v2.83.1
CODER_VERSION=v2.18.3
WRANGLER_VERSION=4.53.0

# Test tools (for CI round-trip validation)
DUMMY_VERSION=v0.0.1
DUMMY_CGO_VERSION=v0.0.1
