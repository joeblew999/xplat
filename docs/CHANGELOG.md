# Changelog

Recent changes to xplat.

*Auto-generated by `xplat internal docs changelog` on 2026-02-03*

## 2026-02-03

- Add tests for syncgh.EnablePages and GetRepoFromRemote (`2fe23ca`)
- Add syncgh.EnablePages for GITHUB_TOKEN support, make images clickable (`5956d87`)
- Add Jekyll templates for all xplat projects (`7160953`)
- Switch to Cayman theme for better layout (`99a9d7d`)
- Switch to Jekyll for GitHub Pages (native .md rendering) (`fea9821`)
- Fix: preserve docs/ path structure for images and links on Pages (`fb60f51`)
- Add automatic GitHub Pages setup to bootstrap (`378266f`)

## 2026-02-02

- Fix: normalize paths after Setup() since it overwrites e.Dir (`49c47f5`)
- Add dev mode to setup action for building from source (`6a83c8c`)

## 2026-02-01

- Fix Windows backslash path corruption in task executor (`e3869df`)

## 2026-01-22

- fix(ci): use local ./xplat for dev:build instead of PATH lookup (`e7807dd`)
- feat: consolidate release builds and reorganize templates (`5b1256c`)

## 2026-01-20

- feat(demo): support multiple fixtures in process demo command (`364014e`)
- feat: add process compose rules, webui enhancements, and playwright testing (`16b90cc`)

## 2026-01-19

- fix(template): use toSlash for unified forward-slash paths (`61c1769`)
- fix(template): use fromSlash for cross-platform path separators (`f590c9a`)
- Use go build -C for cross-platform directory handling (`4072a44`)
- Add fail-fast: false to CI to prevent cascade cancellations (`5b15225`)
- Use PowerShell Push-Location/Pop-Location for Windows builds (`9b098a6`)
- Fix cross-platform dir issues in external taskfile template (`e9a0506`)
- Use git rev-parse for cross-platform status check (`2824943`)
- Add xplat os exists for cross-platform path checks (`5c484cb`)
- Fix cross-platform Taskfile templates and add plat-splat to registry (`e8fcbba`)
- Add xplat gen service and fix registry manifest fetching (`8841bb8`)
- Add node_modules, bun.lock, .releases to gitignore template (`898c484`)
- Update internal templates with consistent DO NOT EDIT banner (`8cb0808`)
- Remove stale plat-geo remote include from Taskfile (`431fed9`)
- Handle 'dev' version in binary download by using latest release (`602c90c`)
- Fix pkg install to parse xplat.yaml manifest format (`0ffafc7`)
- Add prominent GENERATED FILE comments to templates (`8867557`)
- Fix GitHub API rate limit in setup action (`24b2f9e`)
- Add macOS quarantine removal for downloaded binaries (`b02ef01`)
- Fix upload-artifact path for .releases directory (`6a1e057`)
- Add plat-geo to package registry (`ab27e14`)
- Add fallback chain for manifest install (`372501b`)
- Fix error message to show version instead of name-version (`8875859`)
- Fix binary download URL to use standard tag format (`aa10968`)
- Fix release:build to use current platform (`8d89f4c`)
- Add unified CI+release workflow and release:build task (`aa65415`)
- Add unified xplat up command for consolidated web UI (`2712db9`)
- Implement xplat setup check and status commands (`7b97e5b`)
- Add CI verification for generated file sync (`163ac3e`)
- Fix CI action: add error handling for version detection (`fc63291`)
- Add setup wizard CLI and fix CI action install path (`7237635`)
- Refactor: Move inline templates to embed.FS template files (`e37d163`)
- Fix xplat update to always install to ~/.local/bin (`c88d88e`)
- Add startup detection for stale xplat binaries (`fa5928d`)
- Add ADR-016: Single xplat install location (~/.local/bin) (`774671f`)
- Add ADR-015: Hurl for API testing (`6f01c4b`)
- Fix MCP command: correct port and update docs references (`76dd653`)
- Fix ADR-008: Clarify title and correct port number (`1981c0c`)
- Add ADR-014: Consider Huma for API-driven architecture (`91a10f2`)
- Rename 'docs' to 'internal:docs' to clarify it's for xplat devs only (`8144a4d`)
- Add ACTORS.md documenting xplat user types (`0545518`)
- Regenerate docs with new docs all command (`a9d99c4`)
- Add docs all and docs taskfile subcommands (`f0f6245`)
- Use config constants for default ports in ui command (`680045e`)
- Add Claude skills, ADRs, and sync-cf improvements (`a765f57`)
- Add ui command to start Task UI web interface (`72f9f77`)

## 2026-01-15

- docs(adr): add ADR-005 for Cloudflare sync architecture analysis (`1cb9524`)
- chore(workers): upgrade syumai/workers to v0.31.0 (`fe859ba`)
- refactor(os): rename which.go to os_which.go for consistency (`f726be7`)
- feat(task): complete ADR-003 with remaining CLI flags (`6914c54`)
- docs: organize ADRs into adr/ folder with accepted subfolder (`19fe436`)
- feat(task): add --completion, --no-status, --nested flags (`b9c680b`)
- feat(syncgh): add gosmee integration and Task cache invalidation (`e121f23`)

## 2026-01-14

- feat(mcp): add default port config and XPLAT_MCP_PORT env var (`3e234b0`)
- feat(mcp): add HTTP and SSE transport options (`83f99f2`)
- feat(mcp): add embedded MCP server for AI IDE integration (`4db8c37`)

## 2026-01-12

- fix(processcompose): add omitempty tags to avoid invalid yaml output (`bfb5b6b`)
- chore: add .bin/ to gitignore and remove from tracking (`9921f0c`)
- refactor(cli): consolidate gen commands and improve docs (`c946238`)
- feat(workflow): auto-detect language for CI generation (`a196351`)
- Add xplat setup composite action and simplify generated workflows (`c3da968`)
- Use current xplat version in generated CI workflow (`5e817ea`)
- fix: suppress process-compose debug logs, install to ~/.local/bin (`3b9ec95`)
- fix: suppress process-compose debug logs by default (`5ac9262`)
- refactor: DRY checksum fetching with GetExpectedChecksum helper (`f63f4a0`)
- security: add SHA256 checksum verification (`8c558bd`)
- security: add SHA256 checksum verification to install.sh (`8ed8fac`)
- ci: add install.sh test workflow (`629519f`)
- docs: simplify release notes with install.sh (`07d8462`)
- fix(ci): update release workflow tests to use 'xplat os' subcommands (`15e990e`)
- fix(ci): update tests to use 'xplat os' subcommands (`03a05e1`)
- Refactor xplat: remove taskui, add process-compose tools and config (`52b88b7`)

## 2026-01-07

- docs: add Task UI section with screenshots (`cb78033`)
- feat(taskui): add remote taskfile support and grouped sidebar (`b5bf2ba`)
- fix: docs generator uses correct Taskfile command quoting (`2b9fd92`)
- fix: correct Taskfile.yml command quoting syntax (`ce8a29b`)
- docs: add install.sh to README (`5771e05`)
- feat: add install.sh for easy installation (`95da340`)
- Merge feat/taskui-via: Via-only Task UI (`1337751`)
- refactor(taskui): simplify to Via-only mode, remove WebSocket/xterm.js (`7fb2134`)
- feat: add --with-ui flag to service start command (`bf37bbd`)
- feat: add Via/SSE mode for Task UI (--via flag) (`f2a2348`)
- feat: add web-based Task UI (xplat ui command) (`c98520f`)
- fix: use atomic rename for binary updates to prevent corruption (`7ab1965`)
- feat: Add standalone Taskfile.xplat-dev.yml for remote includes (`84fb50f`)
- fix: update CI workflows to use root package after main.go move (`ca154b7`)
- fix: disable TUI for headless service mode (-t=false) (`4b87d06`)
