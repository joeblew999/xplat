# ============================================================================
# GENERATED FILE - DO NOT EDIT MANUALLY
# ============================================================================
# Generated by: xplat gen workflow
# Regenerate with: xplat gen workflow --force
# Source: https://github.com/joeblew999/xplat
# Template: internal/templates/project/ci.yml.tmpl
# ============================================================================
#
# Unified CI/CD workflow - build, test, and release
#
# Triggers:
#   - Push/PR to main: Build + Test + Lint
#   - Tag push (xplat-v*): Build + Test + Release

name: CI

on:
  push:
    branches: [main]
    tags: ['xplat-v*']
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 'stable'
      - name: Bootstrap xplat
        run: go build -o xplat .

      - name: Build
        run: ./xplat task dev:build

      - name: Test
        run: ./xplat task dev:test

      - name: Lint
        run: ./xplat task dev:lint
        continue-on-error: true

      # Release builds only on tags
      - name: Determine version
        if: startsWith(github.ref, 'refs/tags/xplat-v')
        id: version
        run: echo "version=${GITHUB_REF_NAME#xplat-}" >> $GITHUB_OUTPUT

      - name: Build release binaries
        if: startsWith(github.ref, 'refs/tags/xplat-v')
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: ./xplat release build xplat

      - name: Upload release artifacts
        if: startsWith(github.ref, 'refs/tags/xplat-v')
        uses: actions/upload-artifact@v4
        with:
          name: xplat-binaries
          path: .releases/

  release:
    if: startsWith(github.ref, 'refs/tags/xplat-v')
    needs: ci
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: xplat-binaries
          path: release

      - name: Generate checksums
        working-directory: release
        run: sha256sum xplat-* > checksums.txt

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${GITHUB_REF_NAME#xplat-}"
          gh release create "$GITHUB_REF_NAME" \
            --title "xplat ${VERSION}" \
            --generate-release-notes \
            release/*
